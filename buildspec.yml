version: 0.2
env:
  variables:
    S3_BUCKET: ""
phases:
  build:
    commands:
      - npm install
      - npm run build
      - npm run zip
      - ls
      - ls dist
      - aws cloudformation package --template sam.yml --kms-key-id arn:aws:kms:us-east-1:390894576276:key/ace0eb5a-2055-4e32-aeec-908239e7b6e3 --s3-bucket $S3_BUCKET --output-template post-sam.yml
artifacts:
  files:
    - dist/handler.zip
    # The sam.yml file is not part of the lambda function. 
    # It is needed for the CodePipeline Deploy step.  
    - post-sam.yml
    - cicd-pipeline/lambda-params.json  
  discard-paths: yes
